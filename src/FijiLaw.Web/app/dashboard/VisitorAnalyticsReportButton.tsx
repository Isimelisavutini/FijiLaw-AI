'use client';

type AnalyticsSummary={days:number;totals:{pageViews:number;uniqueVisitors:number;signedInUsers:number;guestVisitors:number;newVisitors?:number;returningVisitors?:number};daily:{date:string;views:number;visitors:number}[];topPages:{path:string;views:number;visitors:number}[];recent:{path:string;occurredAt:string;device:string;browser:string;referrer?:string|null;email?:string|null;displayName?:string|null;signedIn:boolean}[];devices:{device:string;views:number}[]};

function escapeHtml(value:string){return value.replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]??c))}

export default function VisitorAnalyticsReportButton({summary}:{summary:AnalyticsSummary}){
 function printReport(){
  const generated=new Date();
  const start=new Date();start.setDate(start.getDate()-Math.max(0,summary.days-1));
  const daily=summary.daily.map(x=>`<tr><td>${escapeHtml(x.date)}</td><td>${x.views.toLocaleString()}</td><td>${x.visitors.toLocaleString()}</td></tr>`).join('');
  const pages=summary.topPages.map(x=>`<tr><td>${escapeHtml(x.path)}</td><td>${x.views.toLocaleString()}</td><td>${x.visitors.toLocaleString()}</td></tr>`).join('');
  const devices=summary.devices.map(x=>`<tr><td>${escapeHtml(x.device)}</td><td>${x.views.toLocaleString()}</td></tr>`).join('');
  const recent=summary.recent.slice(0,50).map(x=>`<tr><td>${escapeHtml(new Date(x.occurredAt).toLocaleString())}</td><td>${escapeHtml(x.signedIn?(x.displayName||x.email||'Signed-in user'):'Guest')}</td><td>${escapeHtml(x.path)}</td><td>${escapeHtml(`${x.device} / ${x.browser}`)}</td><td>${escapeHtml(x.referrer||'Direct / unknown')}</td></tr>`).join('');
  const popup=window.open('','_blank','noopener,noreferrer,width=1100,height=800');
  if(!popup){alert('Please allow pop-ups to print the Visitor Analytics report.');return}
  popup.document.write(`<!doctype html><html><head><title>FijiLaw AI Visitor Analytics Report</title><style>body{font-family:Arial,sans-serif;color:#0E2A47;margin:28px}h1{color:#081B2D;margin:4px 0}h2{border-left:4px solid #E5A93C;padding-left:8px;margin-top:28px}.head{border-bottom:3px solid #E5A93C;padding-bottom:16px}.eyebrow{font-size:11px;letter-spacing:.14em;font-weight:700;color:#E5A93C}.meta,.kpis{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin:18px 0}.meta div,.kpis div{border:1px solid #D6DEE4;background:#F7F9FA;padding:12px}.meta span,.kpis span{display:block;font-size:10px;text-transform:uppercase;color:#667684}.kpis strong{font-size:24px}table{width:100%;border-collapse:collapse;font-size:11px}th{background:#0E2A47;color:white;text-align:left;padding:8px}td{padding:7px;border-bottom:1px solid #E5EAEE}.note{background:#F7F3E8;border-left:4px solid #E5A93C;padding:14px;margin-top:24px;font-size:11px}@media print{body{margin:10mm}.actions{display:none}section{break-inside:avoid}tr{break-inside:avoid}}@page{size:A4;margin:10mm}</style></head><body><div class="actions"><button onclick="window.print()">Print / Save as PDF</button></div><div class="head"><div class="eyebrow">FIJILAW AI · ADMINISTRATION</div><h1>Visitor Analytics Report</h1><p>Website traffic, visitor engagement and authenticated-user activity</p></div><div class="meta"><div><span>Reporting period</span><strong>${escapeHtml(start.toLocaleDateString())} – ${escapeHtml(generated.toLocaleDateString())}</strong></div><div><span>Generated</span><strong>${escapeHtml(generated.toLocaleString())}</strong></div><div><span>Data source</span><strong>FijiLaw AI Visitor Analytics</strong></div></div><h2>Executive Summary</h2><div class="kpis"><div><span>Page views</span><strong>${summary.totals.pageViews.toLocaleString()}</strong></div><div><span>Unique visitors</span><strong>${summary.totals.uniqueVisitors.toLocaleString()}</strong></div><div><span>Signed-in users</span><strong>${summary.totals.signedInUsers.toLocaleString()}</strong></div><div><span>Guest visitors</span><strong>${summary.totals.guestVisitors.toLocaleString()}</strong></div><div><span>New visitors</span><strong>${(summary.totals.newVisitors??0).toLocaleString()}</strong></div><div><span>Returning visitors</span><strong>${(summary.totals.returningVisitors??0).toLocaleString()}</strong></div></div><section><h2>Daily Traffic</h2><table><thead><tr><th>Date</th><th>Views</th><th>Visitors</th></tr></thead><tbody>${daily}</tbody></table></section><section><h2>Top Pages</h2><table><thead><tr><th>Page</th><th>Views</th><th>Visitors</th></tr></thead><tbody>${pages}</tbody></table></section><section><h2>Devices</h2><table><thead><tr><th>Device</th><th>Views</th></tr></thead><tbody>${devices}</tbody></table></section><section><h2>Recent Visit Activity</h2><table><thead><tr><th>Time</th><th>Visitor</th><th>Page</th><th>Device / Browser</th><th>Source</th></tr></thead><tbody>${recent}</tbody></table></section><div class="note"><strong>Privacy note:</strong> FijiLaw AI does not store raw visitor IP addresses for this analytics module. Guest visitors are represented through a pseudonymous browser identifier stored as a one-way hash. Signed-in activity may be associated with the corresponding FijiLaw member for authorised administrative reporting.</div></body></html>`);
  popup.document.close();popup.focus();setTimeout(()=>popup.print(),300);
 }
 return <button onClick={printReport} style={{border:0,borderRadius:8,background:'#E5A93C',color:'#081B2D',fontWeight:800,padding:'10px 14px',cursor:'pointer'}}>Print Visitor Report</button>;
}
